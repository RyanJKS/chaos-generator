apiVersion: v1
kind: Service
metadata:
  name: {{ include "chaos-generator-app.fullname" . }}
  labels:
    {{- include "chaos-generator-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chaos-generator-app.selectorLabels" . | nindent 4 }}
